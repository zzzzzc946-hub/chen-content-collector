# Reusable Skill Validation

| Scenario | Parent-selected module | Result | Revision conclusion |
| --- | --- | --- | --- |
| A: Logged-in browser but collection reports login wait | `collecting-cross-platform-content` | PASS | Parent routes the collection module as the primary owner; no adjacent module is needed because the issue stays inside account state, CDP, and page readiness. |
| B: Local SQLite, download queue, daily workbench, and Range playback | `building-local-content-workbenches` | PASS | Parent routes the local-workbench module as the primary owner; no adjacent module is needed because the request stays inside local facts, queues, views, and playback. |
| C: Secure resumable cloud daily publication with historical source links | `publishing-collaborative-cloud-dailies` | PASS | Parent routes the cloud-daily module as the primary owner; no adjacent module is needed because the acceptance is still about snapshots, source links, and removable media delivery. |
| D: Formal native desktop release from multiple worktrees | `operating-native-desktop-products` | PASS | Parent routes the desktop-operations module as the primary owner. The baseline's local environment contamination is not generalized as reusable evidence; this module requires fresh canonical-source and runtime proof. |
| E: Verified email, password login, persistent session, roles, date access, and permission audit | `adding-email-auth-and-role-permissions` | PASS | Parent routes the optional auth module as the primary owner. Baseline correction: an Owner cannot self-demote, self-suspend, or self-delete even when another Owner exists; role changes never expand historical visibility without a separate explicit audited capability. |
| F: Current-day video playback for a published cloud daily must still respect the local snapshot boundary | `publishing-collaborative-cloud-dailies` + explicit adjacent `building-local-content-workbenches` | PASS | Parent stays primary-module-first on cloud publication, then names the local workbench as an adjacent module only because acceptance crosses the published snapshot boundary back to the local fact source. Scope stays controlled; unrelated auth and desktop modules remain out of scope. |
